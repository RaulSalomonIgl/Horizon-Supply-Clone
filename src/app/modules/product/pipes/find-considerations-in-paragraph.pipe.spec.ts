import { FindConsiderationsInParagraphPipe } from './find-considerations-in-paragraph.pipe';

describe('FindConsiderationsInParagraphPipe', () => {
  it('create an instance', () => {
    const pipe = new FindConsiderationsInParagraphPipe();
    expect(pipe).toBeTruthy();
  });
});
